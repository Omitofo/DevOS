# PROJECTS

**Purpose:** Ephemeral workspace for live projects.

Each project is a self-contained folder that accumulates artifacts through the pipeline.  
When the Master Design Plan is approved, the project can be archived or handed off.

The `_template/` folder provides the exact starting structure for every new project.
