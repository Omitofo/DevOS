# Constitution

This file is the in-repository pointer to the authoritative constitution.

**Authoritative source:**  
`DEVOS_BOOTSTRAP_SPEC.md` (lives outside the DevOS repository)

The bootstrap specification is the single source of truth.  
It is versioned independently of this repository.

Any capable language model, given only that document and a blank workspace, must be able to reconstruct the complete DevOS repository skeleton, philosophy, architecture, agents, quality gates, and operational rules.

Once DevOS exists, the bootstrap specification has completed its primary mission. It lives outside the DevOS repository — exactly as an installer lives outside the operating system it creates.

See the external specification for the full text.
